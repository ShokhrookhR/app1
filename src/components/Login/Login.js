import React from 'react';
import './Login.module.css';
import s from './Login.module.css';
const Login = () => {
  return (
    <div className={s.body}>
      <strong className={s.title}>Login</strong>
    </div>
  );
};

export default Login;
